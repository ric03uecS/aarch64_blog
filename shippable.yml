language: node_js

python:
  - 8.4.0

env:
  - IMAGE_NAME=ric03uec/demoblog:ci.$BUILD_NUMBER

build:
  ci:
    - node --version
    - docker build -t $IMAGE_NAME -f Dockerfile .
    - docker push $IMAGE_NAME

integrations:
  hub:
    - integrationName: aarch64_registry
      type: docker
